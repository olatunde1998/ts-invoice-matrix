

export default function PaymentDetailsPage({ params }: { params: any }) {
  const paymentId = params.id;
  // const projectArray = projectLinks || [];
  // const project = projectArray.find((item) => item?.id === projectId);

  return (
    <div className="px-4 p-2 mb-4 md:p-8 lg:mb-0  font-sans min-h-screen ">
      <div>
        This is PDP (Product Detail Page) coming soon...
      </div>
    </div>
  );
}
